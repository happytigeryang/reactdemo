import React, {Component} from 'react';

export default class AccountLoginPage extends Component {
    state = {}
    render() {
        return (
            <div>this is sign/up sign/in.</div>
        );
    }
}
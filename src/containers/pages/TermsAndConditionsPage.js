import React, {Component} from 'react';
import {getAgreement} from '../../actions/agreementActions';

export default class TermsAndConditionsPage extends Component {
    state = {}

    render() {
        
        store.dispatch({type: ypes.AGREEMENT_FETCH_SUCCESS, payload: 1});
        return (
            <div>Seller Registration
            </div>
        );
    }
}